{
  "name": "@starship-catch-sim/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node .vitepress/sync-report.mjs",
    "dev": "pnpm run sync && vitepress dev",
    "build": "pnpm run sync && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "markdown-it-mathjax3": "^4.3.2",
    "vitepress": "^1.6.4"
  }
}
